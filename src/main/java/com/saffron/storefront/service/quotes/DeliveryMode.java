package com.saffron.storefront.service.quotes;

public enum DeliveryMode {
    /** Wolt Drive / Glovo Courier — on-demand same-day courier in Poland. */
    INSTANT,
    /** DHL / DPD / etc. — parcel courier, 1–7 day transit, worldwide. */
    COURIER
}
